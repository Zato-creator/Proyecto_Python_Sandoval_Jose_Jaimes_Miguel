Proyecto 1 python Se realiza un software en python, donde se ingresan dos tipos de usuarios los admin  o los operario cada uno debe tener permisos distintos para generar diferentes estructuras de opciones, iniciamos con ----app.py----  Donde tenemos la estructura principal de control del progrmaa importamos los modulos cr (importa la logica prinsipal para la persistencia de datos), ut(actua como una bioblioteca que contiene funciones para mejorar la experiencia del usuario en consola), ct(lo uso para importar todo el contenido o funciones del crud contacts), ms (importo el mensaje que contiene l menu de opciones), incorporo el if --- name para declarar el archivo app.py como el principal donde solo se debe ejecutar desde hay. Luego se difine el diccionario principal origin el cual va a recibir todos los datos de los usuarios, creamos un mensaje de bienvenida y el login de incio donde el usuario ingresara sus credenciales para luego ser validades permitiendo el acceso al menu principal, luego se crea la logica de escogencias del menu donde el usuario prodra seleccionar el menu y este valida que solo permita dejar ingresar numeros y contiene las funciones de cada accion a escoger. ---- agenda.json aca creamos nuestra base dedatos para nuestra permanensia de datos conteniendo los siguientes atributos: "id": "0000", "nombres": "apellidos": "telefono": "correo electronico": "direccion": "contrasena": "rol": ----modules---- dentro de esta carpeta creamos las utilidades del programa principal en ella tenemos a core.py, crud_contacts.py, crud_user.py, messages.py, utils,py ----core.py---- en este modulo creamos la persistencia de datos para que se pueda leer escribir en la base de datos. ----crud_contacts---- Creamos toda las funciones para que fueran ejecutadas a la hora de ser llamadas por el archivo app.py prinsipal. ----messages---- en este modulo encontramos el menu visual de nuestros usuarios gurdandolo en uan variable y siendo llamado cuando se requeira en otra funcion en este caso la app.py prinsipal ----utils.py---- se crearon las funciones de pausar y borrar pantalla ademas, se añadiron funcioens de validacioon de caracteries para los diferentes ingresos de datos numericos y str.