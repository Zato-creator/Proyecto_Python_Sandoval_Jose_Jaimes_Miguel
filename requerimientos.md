--auditorias_datos-- le agregue un modulo nuevo auditoria_datos es un archivo que lee toda nuestra base de datos o el archivo llamado .json para revisar si intrudujimos algun dato mal pero me asegure que solo leaa mas no que m,odifique datos. en --messages-- agregue la opcion se  6. Auditar datos. para mostrar el nuevo modulo de nuestro softwar.  
Para el diccionario de Usuarios: Su algoritmo debe iterar sobre los registros y verificar la existencia de todas las llaves obligatorias (id, nombres, apellidos, telefono, email, direccion, password, rol). Necesito que apliquen validaciones de tipo de dato: el teléfono debe ser estrictamente numérico (investiguen cómo validar cadenas de números en Python) y el email debe tener una estructura lógica (como mínimo la presencia del carácter @ y un dominio). Además, deben implementar la lógica necesaria para detectar correos duplicados y asegurarse de que el valor del rol sea estrictamente admin u operario; cualquier otro valor debe ser marcado como una anomalía. --entregable del modulo-- Para el diccionario de Usuarios: Su algoritmo debe iterar sobre los registros y verificar la existencia de todas las llaves obligatorias (id, nombres, apellidos, telefono, email, direccion, password, rol). Necesito que apliquen validaciones de tipo de dato: el teléfono debe ser estrictamente numérico (investiguen cómo validar cadenas de números en Python) y el email debe tener una estructura lógica (como mínimo la presencia del carácter @ y un dominio). Además, deben implementar la lógica necesaria para detectar correos duplicados y asegurarse de que el valor del rol sea estrictamente admin u operario; cualquier otro valor debe ser marcado como una anomalía.